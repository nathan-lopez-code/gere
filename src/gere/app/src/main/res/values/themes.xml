<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Gere" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorAccent">?attr/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorBackgroundSplash</item>

        <!-- status and naviagation bar color inherit with colorPrimaryVariant -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">?attr/colorPrimaryVariant</item>


        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.splash" parent="Theme.Gere" >
        <item name="android:colorBackground">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">?attr/colorPrimaryDark</item>
    </style>

    <style name="Theme.config" parent="Theme.Gere">
        <item name="colorPrimary">@color/colorPrimaryVariant</item>
    </style>
</resources>